apiVersion: runtime.spotify.com/v1alpha1
kind: Runtime
metadata:
  name: {{SERVICE_NAME}}
  namespace: {{NAMESPACE}}
spec:
  owners:
    - {{TEAM}}@iam.spotify.com
  resourceQuota:
    hard:
      limits.cpu: '96'
      limits.memory: 360Gi
      requests.cpu: '48'
      requests.memory: 180Gi
